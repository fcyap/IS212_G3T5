# Server Configuration
PORT=3001
NODE_ENV=development
LOG_REQUEST_ENTRY=true
SESSION_SECRET=a_secure_random_string_for_session_signing

# Database Configuration
# Try pooler connection (often more reliable than direct connection)
DATABASE_URL=postgres

# Alternative direct connection (if pooler doesn't work)
# DATABASE_URL=postgres

# Supabase Configuration
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=anon_key_placeholder
SUPABASE_SERVICE_ROLE_KEY=service_role_key_placeholder
